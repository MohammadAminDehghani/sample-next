export interface StoreTagInterface {
    id: string;
    name: string;
    description: string | null;
}

export interface EditTagInterface {
    id: string;
    name: string;
    description: string | null;
}